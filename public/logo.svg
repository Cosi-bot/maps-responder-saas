<svg width="100" height="100" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Minimalist Geometric Cyberdog Face (Neural Nodes) -->
  
  <!-- Connections -->
  <path d="M50 80 L25 35 L75 35 L50 80" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round" opacity="0.4"/>
  <path d="M25 35 L15 15 M75 35 L85 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
  
  <!-- Neural Nodes -->
  <circle cx="25" cy="35" r="5" fill="currentColor" /> <!-- Left Eye -->
  <circle cx="75" cy="35" r="5" fill="currentColor" /> <!-- Right Eye -->
  <circle cx="50" cy="80" r="7" fill="url(#grad1)" />  <!-- Snout/Main Processor -->
  
  <!-- Definitions -->
  <defs>
    <linearGradient id="grad1" x1="50" y1="73" x2="50" y2="87" gradientUnits="userSpaceOnUse">
      <stop stop-color="#10b981"/>
      <stop offset="1" stop-color="#059669"/>
    </linearGradient>
  </defs>
</svg>
